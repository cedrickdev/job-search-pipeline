"""Switzerland — the reference Country Pack (§9).

Five YAML files and `pack.py`, which is a `load_pack()` call and a cache. That
ratio is the point of Phase 5: everything Switzerland-specific that V1 kept in
`pipeline/discover.py`'s two hardcoded dicts, in `config/searches.yaml`'s two Vaud
towns and in `pipeline/sources/coop.py`'s `_VAUD_HINTS` tuple is data here, and
adding France means adding a sibling directory rather than editing an algorithm.
"""
